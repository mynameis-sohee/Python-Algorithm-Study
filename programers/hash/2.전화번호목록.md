계속 틀림...
```python
def solution(phone_book):
  from itertools import combinations
  for i, j in combinations(phone_book,2):
    if (j.startswith(i)) or (i.startswith(j)):
      answer = False
      print(answer)
    else:
      answer = True
      #print(answer)
  return answer     #이상하게 if와 else 둘다 지나감
  ```
